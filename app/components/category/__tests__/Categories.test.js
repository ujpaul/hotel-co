import React from "react";
import { render } from "@testing-library/react";
import Categories from "../Categories";

test("Categories component renders without errors", () => {
  render(<Categories />);
});
